# Nixpacks build config for the ChessTrophies backend (server/)
# Used by railway.json -> build.nixpacksConfigPath
providers = ["node"]

[phases.install]
cmds = ["npm --prefix server ci"]

[phases.build]
cmds = []

[start]
cmd = "npm --prefix server start"
